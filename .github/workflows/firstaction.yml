name: My first action

on:
  # push 到 master 分支時會觸發
  push:
    branches:
    - master

jobs:
  build:
    name: Set up environment
    runs-on: ubuntu-latest
    container:
      image: cymetricsdev.azurecr.io/security/email-auth
      credentials:
        username: ${{ secrets.dev.USER }}
        password: ${{ secrets.dev.PWD }}
    steps:
    - name: first action
      run: |
        echo $HIO
        echo $GITHUB_REF